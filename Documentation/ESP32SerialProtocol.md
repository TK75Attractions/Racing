# ESP32 シリアルコントローラープロトコル

Unity 側は 115200 bps、改行 `\n` で Player 1 / Player 2 の ESP32 を個別に読み取ります。

## デバイス識別

自動検出を使う場合、Unity はポートを開いた後に次を送信します。

```text
IDENTIFY
```

ESP32 は自身に保存された論理 ID を返してください。

```text
DEVICE,P1
```

または:

```text
DEVICE,P2
```

論理 ID は USB の差し直しで変化しないよう、各 ESP32 のファームウェアまたは不揮発メモリに保存します。

自動識別を実装しない場合は、Unity の `InputManager > Serial Controllers` で各プレイヤーの `Port Name` に `COM3` などを明示します。明示ポートでは識別応答は必須ではありません。

## 入力フレーム

既存の2列形式をそのまま利用できます。

```text
pedal,handle
```

- `pedal`: `-1.0` から `1.0`。正がアクセル、負がブレーキ／後退です。
- `handle`: 既存互換の生値です。Unity 側の `Steering Divisor`（初期値15）で除算されます。

リセットと Ready をESP32から送る場合は、3列目と4列目を追加できます。

```text
pedal,handle,reset,ready
```

- `reset`, `ready`: `1` / `0`、`true` / `false`、`on` / `off` を使用できます。
- 押下イベントを正しく検出するため、ボタンを離したフレームも `0` で送信してください。

既存互換の `||` 以降のデータは無視されます。

```text
0.75,-30,0,1||optional-data
```

入力は30 Hz以上で継続送信してください。初期設定では、有効な入力フレームを2秒間受信できなかったコントローラーを切断状態として扱い、車両入力をニュートラルに戻します。
